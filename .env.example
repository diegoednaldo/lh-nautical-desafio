# Copie este arquivo para .env e ajuste se necessário
# Estas credenciais valem apenas para o ambiente local via Docker Compose

DB_HOST=localhost
DB_PORT=5432
DB_NAME=lh_nautical
DB_USER=lh_user
DB_PASSWORD=lh_password
